[project]
name = "maestro"
version = "0.1.0"
description = "Orchestrator for the problem → Polygon → ElectiCode pipeline"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
